{
  "name": "programmatic-seo-engine",
  "version": "1.0.0",
  "description": "Programmatic SEO engine with Astro + Payload CMS",
  "private": true,
  "scripts": {
    "build": "cd apps/$RAILWAY_SERVICE_NAME && npm install && npm run build",
    "start": "cd apps/$RAILWAY_SERVICE_NAME && npm run start"
  }
}
